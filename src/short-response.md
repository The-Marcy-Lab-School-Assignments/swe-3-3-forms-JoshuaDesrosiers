# Short Response Questions

Answer the following questions in your own words. Each response should be 2-4 sentences.

## Question 1: Accessibility

What is accessibility and why does it matter? Name at least two ways that labels make our form inputs more accessible?

labels allow users to "know" what they are clicking for when they choose a radio or checkbox or even set input
**Your Answer:**

## Question 2: The `name` vs `id` Attribute

`for`, `name` and `id` are attributes we put on form labels and inputs, but they serve different purposes. Explain what each attribute is used for.

**Your Answer:**

id is a unique identifier for styling or js, name is for i have no idea and for is to associate a label to an input

## Question 3: Input Types

Why do we use specific input types like `type="email"` or `type="number"` instead of just using `type="text"` for everything? What advantages do they provide?

**Your Answer:**

Type allows for some auto validation instead of using Js logic or taking invalid input types it filters out some of those issues for us.

## Question 4: Form Submission

Form data is typically sent to a server (a computer that receives the data and does something with it). Provide an example of a real web application that uses a form and, to the best of your ability, explain what the application does with that form data.

When you sign up for any page any website the form sends data to a database or a server which processes the account request based off of the form submission some logic is run off the cloud based off it. Which then sends back some feedback to the webpage or application in use processing our sign-up request. Take instagram for example when you sign up the information you send is pushed to the server as a account creation request of sorts and the result is rendered either in the form of a success and redirect or an error.

**Your Answer:**